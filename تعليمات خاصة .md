تم استخراج البيانات من الملف الأساسي الحقيقي (Dim_KPI + Dim_Owners + Fact_KPI_Measurements) بدل الاعتماد على الصور، وأعدت بناء الصفحة بمعمارية جديدة تجعل ملف CSV هو مصدر البيانات الوحيد.

ما تم تنفيذه:

القسم: استخرجته من Dim_Owners عبر owner_ID في Dim_KPI — الآن كل مؤشر له قسم حقيقي (قسم رياض الأطفال، قسم الموهوبين، إدارة جودة الخدمات... إلخ).
مستهدف جدة / مستهدف وطني: استخرجتهما كقيم خام من Fact_KPI_Measurements حيث Value_Type = Target_Jeddah / Target_National، مع تفضيل هدف السنة المالية الأحدث (FY2025) عند توفره.
إنجاز المستهدف والحالة: لم أعتمد على أي رقم من الصور إطلاقًا — الصفحة نفسها تحسبهما مباشرة في المتصفح: آخر قيمة فعلية ÷ المستهدف × 100، والحالة محقق إذا بلغت 100% فأكثر، وغير محقق إذا أقل. بما أن الحساب يحدث في الصفحة نفسها من القيم الخام، فتحديث أي رقم في CSV (قيمة ربع، أو مستهدف) يُعاد حساب الإنجاز والحالة تلقائيًا دون أي تدخل يدوي إضافي.
مصدر بيانات الصفحة الآن ملف kpi_master_data.csv خارجي يُقرأ عبر fetch() بنفس ترتيب أعمدة الجدول، بدل تضمين البيانات داخل كود HTML — بمجرد تعديل CSV وحفظه، وإعادة تحميل الصفحة (F5)، تظهر القيم الجديدة فورًا دون لمس كود الصفحة.
أضفت شاشة تحميل وشاشة خطأ واضحة داخل الصفحة نفسها تشرح للمستخدم سبب أي مشكلة في القراءة.

⚠️ تعليمات مهمة جدًا لتشغيل الصفحة بدون مشاكل:

المتصفحات (خصوصًا Chrome وEdge) تمنع قراءة ملفات CSV بأمر fetch عند فتح الصفحة مباشرة بالنقر المزدوج (رابط يبدأ بـ file://) لأسباب أمنية (CORS) — ستظهر رسالة خطأ داخل الصفحة نفسها إن حدث هذا. الحل:

ضع الملفين kpi_dashboard.html وkpi_master_data.csv في نفس المجلد تمامًا (نفس المسار).
شغّل خادمًا محليًا بسيطًا بدل فتح الملف مباشرة، أسهل طريقة:
افتح موجّه الأوامر (Terminal/CMD) داخل نفس المجلد.
نفّذ: python -m http.server 8000 (إن كان لديك بايثون مثبتًا على جهازك).
افتح المتصفح على: http://localhost:8000/kpi_dashboard.html
أو استخدم إضافة Live Server في VS Code إن كنت تستخدمه.
أو الأفضل للاستخدام الدائم: ارفع الملفين إلى استضافة داخلية (SharePoint أو خادم الوزارة الداخلي)، وسيعمل عندها بشكل طبيعي دون أي إعداد إضافي.

للتحديث المستقبلي: عدّل فقط القيم الخام في kpi_master_data.csv (الحالة، الأرباع الخمسة، مستهدف_جدة، مستهدف_وطني) — لا تحتاج لمس عمودي "إنجاز" و"الحالة" فيه؛ هما للرجوع اليدوي فقط، بينما الصفحة تعيد حسابهما بنفسها من القيم الخام.